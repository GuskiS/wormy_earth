{
  "name": "mono-backend",
  "version": "1.0.0",
  "author": "GuskiS <edvards@lazdans.lv>",
  "license": "MIT",
  "private": true,
  "main": "dist/server.js",
  "scripts": {
    "start": "ts-node-dev --type-check --no-notify --respawn --ignore-watch node_modules --watch .env scripts/server.ts",
    "test": "echo \"Error: run tests from root\" && exit 1"
  },
  "dependencies": {
    "fastify": "2.15.1",
    "fastify-cors": "3.0.2",
    "fastify-helmet": "3.0.2",
    "fastify-sensible": "2.1.1",
    "socket.io": "2.3.0",
    "tsconfig-paths": "3.9.0"
  },
  "devDependencies": {
    "@types/socket.io": "2.1.4",
    "pino-pretty": "3.6.1",
    "ts-node-dev": "1.0.0-pre.44"
  }
}
